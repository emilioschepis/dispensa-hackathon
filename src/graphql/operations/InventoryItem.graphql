query InventoryItem($inventoryId: uuid!, $productId: uuid!) {
  inventory_products_by_pk(inventory_id: $inventoryId, product_id: $productId) {
    quantity
    product {
      id
      name
      code
    }
  }
}
